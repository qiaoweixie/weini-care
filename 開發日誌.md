# 開發日誌

最新的在最上面。每次開工先看這裡，知道上次做到哪、接下來要做什麼。

---

## 2026-09-18

在原本的電腦（Windows，含 docs/ 與 prototypes/）接續。開工 `git pull` 拿到 09-17 的全部改動，沒有衝突。

### ä»å¤©å®æ

**文章區改為自動同步 Blogger（同日）**
- 首頁 Journal 區不再放靜態卡片，改由頁尾腳本讀 Blogger feed，只抓標籤「午睡空間」的最新三篇，顯示縮圖（放大到 640px）、日期、標題、前 72 字摘要，點進去看部落格。
- feed 走 `https://www.blogger.com/feeds/4302484093245238516/...`（blog ID 固定），不走 blog.weini.tw，因為後者還沒開 HTTPS，會被瀏覽器擋。
- 還沒有文章貼標籤時顯示備援區「文章整理中 → 前往部落格」，8 秒沒回應也維持備援區，頁面不會卡。
- 卡片固定顯示「巧薇的部落格」，不帶入文章的其他標籤，避免產品名稱（妮美龍、兩用被等）出現在午睡空間首頁。
- 測試方式：網址加 `?journal=標籤名稱` 可改抓其他標籤，例如 `?journal=睡前儀式`。
- **巧薇要做**：到 Blogger 把要出現在首頁的文章加上標籤「午睡空間」。挑文章時避開有療效字眼的（改善發炎、睡眠障礙等）。
- 部落格 HTTPS：blog.weini.tw 憑證已於同日簽發（Google Trust Services），首頁三處部落格連結與腳本已改回 https。Blogger 的「HTTPS 重新導向」可以開啟。
- 品牌名依巧薇指示定為「妮好，薇笑生活｜午睡空間」（薇）。Blogger 標題是「妮好，薇笑人生」，人生／生活的差異巧薇未要求統一。

**åçåç¨±æ¹çºãå¦®å¥½ï¼èç¬çæ´»ï½åç¡ç©ºéãï¼åæ¥ï¼**
- 抬頭改成兩行：主名「妮好，薇笑生活」、副名「午睡空間 · RECHARGE LOUNGE」（手機只顯示「午睡空間」）。
- title、meta description、og:site_name、footer、版權、隱私頁蒐集者、404 標題、README、分享圖一併更新，分享圖已重產。
- 內文句子如「午睡空間不是醫療服務」維持原樣，那是服務名稱不是品牌名。
- 注意：Blogger 的部落格標題是「妮好，薇笑人生」（薇），網站用的是巧薇指定的「微笑生活」（微），兩邊不一致，待巧薇決定要不要統一。

**èªè¨ç¶²å www.weini.twï¼åæ¥ç¨æï¼**
- 巧薇的 weini.tw 原本綁 Blogger，已改成：`www.weini.tw` → 午睡空間（GitHub Pages）、`blog.weini.tw` → Blogger 舊部落格。DNS 在 PChome 買網址代管，兩筆 CNAME 已生效。
- repo 新增 `CNAME` 檔（www.weini.tw）。首頁、隱私頁、sitemap、robots 的正式網址與 404 的絕對路徑全部改為 www.weini.tw 根目錄。舊的 qiaoweixie.github.io/weini-care/ 會自動轉址。
- **尚未處理**：不加 www 的 `weini.tw` 目前指到 PChome 停放頁，因為 PChome 介面填不了 A 紀錄。待確認 PChome「類型」下拉有哪些選項，或改用「轉址」欄把 weini.tw 轉到 www.weini.tw。
- 推送後要到 GitHub Settings → Pages 等 DNS 檢查通過，勾 Enforce HTTPS。


**Open Graph 分享圖與正式網址**
- 新增 `assets/images/og-cover.png`（1200×630，154KB）。原始頁在 `assets/images/source/og-cover.html`，用 Chrome 無頭模式產生，重產指令寫在該檔案開頭的註解。
- `index.html` 的 `og:url`、`og:image`、`twitter:image`、`canonical` 全部改成 https://qiaoweixie.github.io/weini-care/ ，並補 `og:image:width/height/alt`。
- 日後換自訂網域時，要一起改的位置：`index.html` 開頭（有註解列出）、`privacy.html` 的 canonical、`sitemap.xml`、`robots.txt`、`404.html` 的絕對路徑。

**隱私權告知頁**
- 新增 `privacy.html`，依個資法第八條寫：蒐集者、目的、資料類別、利用期間地區對象方式、當事人權利、不提供的影響、第三方服務（Google 試算表、GitHub Pages、Google Fonts）、Cookie。
- 兩個表單的同意文字改成「我已閱讀個人資料蒐集告知，並同意…」並連到該頁，頁尾快速連結也加了「個資告知」。
- **請巧薇確認**：頁面寫「處理完畢後一年內刪除」，這是對外承諾，若做不到請改成其他期限。

**SEO 小檔案**
- `robots.txt`：允許全部，排除 `versions/` 與 `google-sheet/`，指向 sitemap。
- `sitemap.xml`：首頁與隱私頁。
- `404.html`：品牌樣式。因為 404 可能在任何路徑被載入，資源一律用 `/weini-care/` 開頭的絕對路徑，換網域時要改。

**其他**
- 本機 `docs/content-worksheet.md` 已同步 09-17 的版面與表單決策（第 8 節）。

### 還沒做的（依優先順序）

1. **試算表測試列**：09-17 留下的「測試（可刪除）」列與測試通知信，由巧薇自己刪。
2. **空間實拍照**：向逗留工作室、星心相印索取授權或自拍，尺寸見 `versions/photo-layout/還原說明.md`。拿到後照該說明還原有照片版，還原時記得把 09-18 的表單同意文字與 OG 標籤改動一起搬過去。
3. **時段選項**：預約表單目前是「上午／下午／傍晚／都可以」，若有固定場次改成實際時間。
4. **Journal 三篇文章**：目前都標「文章整理中」。
5. **隱私頁保存期限**：見上方請巧薇確認的項目。

---

## 2026-09-17

在另一台電腦（Windows）用 zip 下載的專案接續開發，收工時已接回 GitHub repo 並 push。

### 今天完成

**帳號與連結**
- 全站 Instagram 統一為 @weini.care，舊的 @recharge_lounge_ 已全部移除。
- 抬頭原本的 LINE 圖示改成 IG，兩個重複的 IG 圖示合併為一個。
- 抬頭按鈕與首屏主按鈕改為「預約體驗」，直接跳到預約表單。

**巧薇照片**
- Canva 匯出的 7MB SVG 合成去背後轉成 `assets/images/founder.webp`（145KB）。
- 首屏主視覺改用這張去背照，放在腮紅色塊裡。

**無照片版面（暫時）**
- 目前沒有空間實拍照，改成：About 用品牌色圖形 `soft-blush.svg`、Space 改成四張資訊卡、Founder 只留文字。
- 有照片的版面完整備份在 `versions/photo-layout/`，內含還原說明。等實拍照到位就照那份說明改回去。
- 占位圖上的「台北・台中」已修正為「台北・桃園」。

**預約表單（新）**
- 位置在 #reserve 區塊。欄位：稱呼、聯絡方式、地點（台北／桃園單選）、希望日期（限今天以後）、希望時段（可複選）、備註、同意。
- 人數固定一位，因為一個空間只有一張床。
- 有隱藏欄位擋機器人。

**兩個表單接 Google 試算表（已上線）**
- Apps Script 程式在 `google-sheet/Code.gs`，設定步驟在 `google-sheet/設定步驟.md`。
- 端點已貼進 `index.html` 的 `SHEET_ENDPOINT`，預約與聯絡表單都實測成功寫入，並寄通知信到 qiaoweixie@gmail.com。
- 若端點留空，表單會退回「產生訊息 → 複製 → IG 私訊」的備援流程，網站不會壞。

### 明天接續（用原本的電腦）

1. 開工先 `git pull`，會拿到今天的全部改動。
2. 打開 Google 試算表「午睡空間｜表單」，刪掉今天測試留下的「測試（可刪除）」列，預約分頁可能有兩到三列、聯絡分頁一列。信箱裡的測試通知信也可以刪。
3. 原電腦的 `docs/` 與 `prototypes/` 不在 repo 裡，不受影響，但今天的版面決策（無照片版、備份在 versions/）可以順手記進 `docs/` 的上線清單。

### 還沒做的（依優先順序）

1. **Open Graph 分享圖與正式網址**：`index.html` 開頭的 `og:url`、`og:image` 還是 example.com，且 `og-cover.jpg` 不存在。需要一張 1200×630 的分享圖，網址先用 https://qiaoweixie.github.io/weini-care/ 也可以。
2. **隱私權告知頁**：兩個表單的同意文字寫著「正式上線時將連結完整個資蒐集告知」，目前沒有頁面可連。
3. **空間實拍照**：向逗留工作室、星心相印共享空間索取照片授權，或自己拍。需要的尺寸見 `versions/photo-layout/還原說明.md`。拿到後照該說明還原有照片版。
4. **時段選項**：預約表單目前是「上午／下午／傍晚／都可以」，若有固定場次可以改成實際時間。
5. **Journal 三篇文章**：目前都標「文章整理中」。
6. **SEO 小檔案**：`robots.txt`、`sitemap.xml`、`404.html`。

### 注意事項

- 改 `google-sheet/Code.gs` 之後要在 Apps Script 重新部署「新版本」才會生效，網址不變。
- `versions/` 與 `google-sheet/` 是刻意放進 repo 的，兩台電腦都拿得到。不含個資。
- Apps Script 的網址本身不算機密，但試算表內容有訪客個資，不要把試算表設成公開。
