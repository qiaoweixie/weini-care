# 開發設定｜在另一台電腦繼續開發

網站是純靜態 HTML，沒有後端與資料庫。程式碼在 GitHub，線上網站由 GitHub Pages 自動部署。

- Repo：https://github.com/qiaoweixie/weini-care
- 線上網址：https://qiaoweixie.github.io/weini-care/

---

## 1. 先裝好工具

- **Git**：必裝。https://git-scm.com/
- **Node.js**：選裝，只有要用本機預覽伺服器才需要。https://nodejs.org/

## 2. 複製 repo

在想放專案的資料夾開終端機：

```bash
git clone https://github.com/qiaoweixie/weini-care.git
cd weini-care
```

## 3. 設定 git 身分（新電腦第一次）

```bash
git config user.name "qiaoweixie"
git config user.email "qiaoweixie@gmail.com"
```

## 4. 本機預覽

裝了 Node 之後：

```bash
node .claude/serve.js
```

開啟 http://127.0.0.1:8765 。或用任何靜態伺服器指向專案根目錄。

## 5. 日常流程（每台電腦都一樣）

開工前先拉最新版，避免兩台電腦互相衝突：

```bash
git pull
```

改完後：

```bash
git add -A
git commit -m "說明改了什麼"
git push
```

第一次 `git push` 會跳出瀏覽器要你登入 GitHub，用自己的 `qiaoweixie` 帳號授權即可。推上去後 GitHub Pages 會自動重新部署，一到兩分鐘後線上網站更新。

養成「開工先 pull、收工先 push」的習慣，就不會遇到合併衝突。

---

## 內部文件不會跟著 repo 走

`docs/`（品牌文件、上線前補齊清單）和 `prototypes/`（舊版原型）刻意排除在 repo 外，因為 repo 是公開的。`git clone` 不會拿到這兩個資料夾。

需要的話，用隨身碟或雲端硬碟從原電腦的 `D:\projects\weini_care\docs` 與 `prototypes` 另外複製。網站開發與部署不需要它們。

**不要**把這兩個資料夾加回版本庫，否則會出現在公開 repo。

---

## 常見問題

**push 出現 403 Permission denied**
電腦記住了別的 GitHub 帳號。清掉舊憑證後再推一次，瀏覽器會重新要求登入：

```bash
printf "protocol=https\nhost=github.com\n\n" | git credential reject
git push
```

**push 被拒，說要先整合遠端變更**
另一台電腦推過新版本。先拉再推：

```bash
git pull
git push
```

**改了檔案但線上沒更新**
確認已經 `git push`，然後等一到兩分鐘。若還是舊的，瀏覽器強制重新整理（Ctrl + F5）。

---

## 檔案結構

```
index.html            首頁（單頁式）
assets/css/site.css   樣式與 design token
assets/images/        LOGO、favicon 與占位圖
.claude/serve.js      本機預覽伺服器
開發設定.md           本文件
```

## 上線前仍待補

- LINE 官方帳號連結：抬頭與手機選單的 LINE 圖示，目前暫連到聯絡表單。
- 空間實拍照與巧薇本人照：目前是占位圖，在 `assets/images/placeholder-*.svg`。
- Open Graph 分享圖與正式網域：`index.html` 開頭的 `og:url`、`og:image` 還是 example.com。
- 聯絡表單後端：目前送出只顯示提示，不會真的寄出。
